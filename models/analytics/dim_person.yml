version: 2

models:
  - name: dim_person
    tests:
      - dbt_utils.equal_rowcount:
          compare_model: "`duckdata-320210.wide_world_importers.application__people`"
          config: 
            warn_if: "!= 1" # add_undefined_record
            error_if: "!= 1"

    columns:
      - name: person_id
        tests:
          - unique
          - not_null

      - name: person_full_name
        tests:
          - not_null
